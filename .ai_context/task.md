# Fixing KeyErrors in battery_capacity_prediction.ipynb

- [x] Locate the code block that throws the `KeyError: 'ocv_deviation'`.
- [x] Write a fix to correctly generate or preserve `ocv_deviation` before it is used.
- [x] Apply the fix to Phase 10-3-1 (Cell 84).
- [x] Fix the lowercase capacity error in Phase 10 boxplots.
- [x] Trace and fix the `KeyError: 'Cluster_Pred'` in Phase 11-4 ARI scoring.
- [ ] Have the user verify the execution from Phase 11-2 onwards.
